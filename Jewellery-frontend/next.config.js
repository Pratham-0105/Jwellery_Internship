/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow images from any domain (update with real domains later)
  images: {
    remotePatterns: [],
  },
};

module.exports = nextConfig;
